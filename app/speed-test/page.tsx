"use client"
import { useState } from "react"
import { Gauge, Play, CheckCircle2 } from "lucide-react"
import { SiteShell } from "@/components/layout/site-shell"
import { Container } from "@/components/layout/container"
import { Button } from "@/components/ui/button"
export default function Page() { const [running, setRunning] = useState(false); const [done, setDone] = useState(false); function run(){ setRunning(true); setDone(false); window.setTimeout(()=>{setRunning(false);setDone(true)}, 1400) } return <SiteShell><Container className="py-20 sm:py-28"><p className="font-mono text-xs uppercase tracking-[0.28em] text-accent">03 / SPEED TEST</p><h1 className="mt-5 max-w-3xl text-balance text-5xl font-semibold tracking-tight sm:text-7xl">Measure the route before you deploy.</h1><p className="mt-6 max-w-2xl text-lg leading-relaxed text-muted-foreground">Run a browser-side demo test against our nearest edge. Results are indicative and depend on your current network.</p><div className="glass mt-12 max-w-2xl rounded-2xl p-8"><div className="flex items-center gap-3"><Gauge className="size-6 text-accent" /><div><p className="font-semibold">India edge diagnostic</p><p className="text-sm text-muted-foreground">Mumbai · Delhi · Bengaluru · Hyderabad</p></div></div><Button onClick={run} disabled={running} className="mt-8 w-full">{running ? "Measuring route..." : <><Play data-icon="inline-start" /> Run speed test</>}</Button>{done && <div className="mt-6 grid grid-cols-3 gap-3 text-center"><div className="rounded-xl bg-accent/10 p-4"><p className="font-mono text-2xl text-accent">12 ms</p><p className="mt-1 text-xs text-muted-foreground">latency</p></div><div className="rounded-xl bg-accent/10 p-4"><p className="font-mono text-2xl text-accent">412 Mbps</p><p className="mt-1 text-xs text-muted-foreground">download</p></div><div className="rounded-xl bg-accent/10 p-4"><CheckCircle2 className="mx-auto size-6 text-accent" /><p className="mt-1 text-xs text-muted-foreground">route ready</p></div></div>}</div></Container></SiteShell> }
